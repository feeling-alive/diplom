# Dev/test-only dependencies for the FinTrack backend.
# Install: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
aiosqlite>=0.20   # in-memory async DB for DDL/create_all smoke tests
