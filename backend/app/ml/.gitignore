# Model/scaler binaries are not committed — drop scaler.pkl here locally.
# pytorch_model.pt is auto-downloaded from HF Hub on backend startup (main.py).
# See README.md for how to generate the scaler.
*.pkl
*.pt
!.gitignore
!README.md
