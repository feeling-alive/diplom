# FinTrack backend — FastAPI + Redis proxy/cache for market quotes
# Install: pip install -r requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.34
redis>=5.2
httpx>=0.28
python-dotenv>=1.1
pydantic-settings>=2.7

# --- Database / ORM / migrations (Блок A) -----------------------------------
sqlalchemy[asyncio]>=2.0
asyncpg
alembic

# --- Auth / uploads (Блок B: JWT + Google OAuth; аватары — позже) ------------
# bcrypt напрямую (passlib 1.7.4 несовместим с bcrypt>=4.1/5.x).
bcrypt
python-jose[cryptography]
python-multipart
email-validator     # требуется для pydantic EmailStr (Блок B)
aiofiles
pillow
