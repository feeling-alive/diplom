# Evolution: 2026-05-13 12:30

## Intelligence Summary
- Patches analyzed: 0 (first run — no patch files exist yet)
- Codebase/session conventions captured: 10
- Tech stack: React 18 + TypeScript strict + Vite + Framer Motion + react-router-dom v7 + lightweight-charts v5 + Vitest + RTL

## Improvements Applied

### aif-implement → skill-context
All 10 rules written to `.ai-factory/skill-context/aif-implement/SKILL.md`

1. Framer Motion: `backgroundColor` not `background` in animation props
   ← driven by: codebase bug (AssetTable.tsx TS2590 error)

2. Test isolation: components using `useNavigate` need Router context
   ← driven by: codebase bug (AssetTable.test.tsx + MarketOverview.test.tsx crashes)

3. Test assertions: `getByRole('heading')` when title matches NavLink label
   ← driven by: codebase bug (MarketOverview.test.tsx multiple-match error)

4. Test assertions: `getAllByText` for text appearing in multiple sections
   ← driven by: codebase bug (MarketOverview.test.tsx Объём 24ч + BTC-USDT)

5. lightweight-charts v5: `addSeries(CandlestickSeries)` replaces `addCandlestickSeries()`
   ← driven by: lightweight-charts v5 API change (typings analysis)

6. lightweight-charts: PricePoint timestamps are ms — convert to seconds
   ← driven by: codebase type analysis (PricePoint.timestamp + Time type)

7. react-router-dom NavLink: `end` prop required on root path
   ← driven by: codebase convention (FinTrackNavBar.tsx)

8. react-router-dom NavLink mock: handle function-style style prop
   ← driven by: codebase convention (MarketOverview.test.tsx)

9. Windows Bash tool: `rm` not `del` for file deletion
   ← driven by: session shell error on Windows

10. Vite extension conflict: `.jsx` silently shadows `.tsx`
    ← driven by: session bug (App.jsx + App.tsx coexistence)

## Patterns Identified
- react-router-dom hooks in components require test setup: 2 occurrences
- Same text in multiple rendered sections breaks getByText: 3 occurrences
- TypeScript strict mode surfaced Framer Motion casting issue: 1 occurrence
- Third-party library API version mismatch (lightweight-charts v4→v5): 1 occurrence
- Windows shell/POSIX incompatibility: 1 occurrence
