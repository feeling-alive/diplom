# Evolution: 2026-05-14 18:30

## Intelligence Summary
- Patches analyzed: 0 (no patch files exist; cursor not advanced)
- Existing skill-context: 1 file (aif-implement) with 10 rules — all still valid vs base SKILL.md (no Case A/B/C overlap)
- Codebase/session conventions captured this run: 5
- Tech stack: React 19 + TypeScript strict + Vite + Framer Motion + react-router-dom v7 + Recharts + @tanstack/react-query + lucide-react 1.14

## Improvements Applied

### aif-implement → skill-context
Added 5 rules to `.ai-factory/skill-context/aif-implement/SKILL.md` (now 15 rules total).

1. **lucide-react@1.14.0: no brand icons** (`Github`, `Twitter`, etc.)
   ← driven by: session error this run (CoinInfoBlock TS2305 on Github/Twitter import)
   File: `.ai-factory/skill-context/aif-implement/SKILL.md`

2. **Page layout: use `main-content`, never `app-page` wrapper**
   ← driven by: codebase convention after Market/Asset/Dashboard layout fixes
   File: `.ai-factory/skill-context/aif-implement/SKILL.md`

3. **Public unauthenticated APIs → React Query with 30+ min staleTime**
   ← driven by: codebase pattern in useCoinInfo / useOHLCV / useNews
   File: `.ai-factory/skill-context/aif-implement/SKILL.md`

4. **Bash tool on Windows: POSIX shell only — never PowerShell syntax**
   ← driven by: session error this run (Test-Path/if-not in Bash → syntax error)
   File: `.ai-factory/skill-context/aif-implement/SKILL.md`

5. **Asset/price components: `usePrices()` hook, not static `MOCK_PRICES` import**
   ← driven by: codebase bug — Market components had frozen prices fixed this session
   File: `.ai-factory/skill-context/aif-implement/SKILL.md`

## Stale Rule Decisions
None — all 10 existing rules pass Step 3 check (no overlap with base SKILL.md).

Note: rules #5/#6 (lightweight-charts) reference code removed this session
(CandlestickChart.tsx) but remain informational — kept per Rule 11 "no losing coverage".

## Patterns Identified
- Third-party library version mismatch (lucide-react 1.x dropped brand icons): 1 occurrence, repeat risk
- Layout double-wrapper (`app-page` inside `ProtectedLayout`): 3 occurrences before fix this session
- Static MOCK_PRICES import freezing live data: 3 occurrences (MarketSummaryBar, TopMovers, AssetTable)
- Windows shell incompatibility (PowerShell vs POSIX bash): 2 occurrences across sessions
- React Query as caching layer for free public APIs: 4 occurrences (becomes a convention)

## Cursor State
Cursor `<paths.evolutions>/patch-cursor.json` not advanced — zero new patches processed.
