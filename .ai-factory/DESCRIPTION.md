# FinTrack — финансовый дашборд (дипломный проект)

## Обзор

FinTrack — настраиваемый финансовый дашборд для отслеживания крипто-, фондовых и форекс-активов в реальном времени. Дипломный проект (ВятГУ, ИСПк-402-52-00). Цель — впечатляющая демонстрация для руководителя: гибкий drag-and-drop дашборд в стиле iPhone, страница актива с TradingView-графиком и AI-чатом, агрегированные рыночные данные из публичных API.

## Ключевые функции

- **Кастомизируемый дашборд** — 20+ типов виджетов (KPI, watchlist, price chart, news, allocation, top movers, fear & greed, forex rates и др.), iPhone-style drag-and-drop, ресайз за край с овальным индикатором размера, карусель страниц, edit mode.
- **Страница актива** (`/asset/:symbol`) — TradingView Advanced Chart Widget в fullscreen, Recharts area-чарт, метрики со скроллом, CoinGecko-описание монеты, новости.
- **Market Overview** (`/market`) — сводка рынка, top movers, таблица активов со sparklines.
- **AI-чат** — бэкенд-endpoint `POST /api/chat/message`: авторизация JWT, свечи OHLCV → локальный PyTorch PatchTST (прогноз UP/DOWN/SIDEWAYS) → Groq Llama 3.3 (ответ на русском), сохранение истории в `ChatSession`, кэш прогноза в Redis (60s), graceful degradation на каждом этапе.
- **Источники данных в реальном времени** — OKX WebSocket (крипто), Finnhub (акции), Frankfurter (форекс), NewsAPI (новости), CoinGecko (описание монет). Vite-прокси обходит CORS.
- **Авторизация** — LoginPage / RegisterPage / ProfilePage / SubscriptionPage / AdminPanelPage с `PrivateRoute`-гвардом.
- **Персистентность раскладки** — `localStorage` (`fintrack_widgets_v2`) с версионированием ключа и клампом по схеме виджет-реестра.

## Технологический стек

- **Язык:** TypeScript 6 (strict, `@typescript-eslint/no-explicit-any: error` — никаких `any`)
- **Сборка:** Vite 8 + `@vitejs/plugin-react`
- **UI-фреймворк:** React 19
- **Маршрутизация:** react-router-dom v7 (layout route с `<Outlet />`)
- **Состояние / данные:** TanStack Query 5, локальный `useState` + `localStorage`
- **Грид и DnD:** react-grid-layout (компактный по вертикали), react-resizable; `@dnd-kit/core`+`sortable` — резерв для других сценариев
- **Графики:** Recharts (area, pie), lightweight-charts v5 (свечи на странице актива), TradingView Advanced Chart Widget (через iframe)
- **Анимация:** Framer Motion (страничные переходы, hover, stagger)
- **Иконки:** lucide-react (никаких emoji в production-коде)
- **AI:** Groq REST API (env `VITE_GROQ_API_KEY`)
- **Тесты:** Vitest 4 + React Testing Library + jsdom (48+ тестов)
- **Линтер:** ESLint 10 flat config (`recommended` + `react-hooks` + `react-refresh` + ts-плагин)

## Заметки по архитектуре

- **Структура `src/`:** `pages/` (роуты), `components/{layout,dashboard,market-overview,asset}/`, `hooks/` (один файл на источник данных), `mock/` (fallback-данные), `types/` (market.types, widgets.types), `utils/` (format helpers), `lib/` (env), `config/`, `constants/`, `data/` (JSON-снимок 46 активов).
- **Хуки:** каждый принимает `useMock?: boolean = true` для разработки без ключей API. Поллинг — таймеры внутри хука (Finnhub 60s, прайсы 15s + jitter). OKX — WebSocket.
- **Виджеты:** описываются в `WIDGET_REGISTRY` (`src/types/widgets.types.ts` + соответствующий объект) — `minW/maxW/minH/maxH` обязательны на каждом элементе layout (иначе react-grid-layout не блокирует ручки ресайза).
- **Дизайн-система:** строгая палитра 11 цветов (см. `.ai-factory/RULES.md`), типографика Inter, радиусы 8/12/16/24/999, набор теней sm/md/lg.
- **Прокси:** `vite.config.ts` маршрутизирует `/api/finnhub`, `/api/news`, `/api/forex` на внешние API без раскрытия ключей в браузере.
- **Бэкенд (с 2026-05-30):** отдельный сервис `backend/` (FastAPI + Redis, Python 3.13) — прокси/кэш котировок (Finnhub/OKX/Frankfurter) под `/api/quotes`, TTL 60/30/300с, graceful degradation. Запуск: `uvicorn app.main:app --port 8000`. Миграция хуков фронта на него пока отложена.
- **AI-чат (с 2026-06-05):** эндпоинт `POST /api/chat/message` — OHLCV свечи → PatchTST (прогноз UP/DOWN/SIDEWAYS) → Groq Llama 3.3 (фин. аналитик на русском). Кэш прогноза в Redis (60s), сохранение истории в `ChatSession` (мердж по symbol+user). Graceful degradation на каждом этапе. Сервисы: `services/patchtst.py`, `services/groq_service.py`, `routes/chat.py`.
- **Локальный инференс PatchTST (с 2026-06-10):** инференс переведён с HF Inference API на **локальный PyTorch** — архитектура модели восстановлена из обучающего ноутбука в `services/patchtst_model.py` (PatchTST: patch_len=12, stride=6, d_model=64, 2 слоя TransformerEncoder, бинарный классификатор UP/DOWN), веса `app/ml/pytorch_model.pt` грузятся лениво (`torch.load(weights_only=True)`), скейлер — `app/ml/scaler.pkl` через joblib. Подготовка признаков (60×11: OHLCV + RSI/MACD/BB на numpy) — внутри `services/patchtst.py::_build_features`. `source` прогноза: `"local"` (был `"huggingface"`); кэш в Redis пишется только для `source=="local"`. `hf_api_key`/`hf_model_id` удалены из конфига; `huggingface_hub` остался только для авто-скачивания весов при старте (`main.py` lifespan, graceful при оффлайне). Новая зависимость: `torch` (CPU). Модель бинарная — genuine SIDEWAYS невозможен, боковик порождается только confidence-gate.
- **Апгрейд ИИ-модуля (с 2026-06-05):** параметры инференса вынесены в конфиг (`prediction_seq_len`/`prediction_timeframe`/`prediction_confidence_threshold`/`prediction_margin`, `news_context_limit`, `general_news_enabled`). Подготовка признаков + опц. нормализация скейлером — `services/features.py` (ленивый `joblib`-load `app/ml/scaler.pkl`, graceful fallback на сырые close-цены; новые зависимости `numpy`/`joblib`/`scikit-learn`, импортируются лениво). **Порог уверенности**: слабый `UP`/`DOWN` (ниже порога или с малым margin над runner-up) понижается до `SIDEWAYS` с флагом `low_confidence` — решает «проблему боковика». Новости в чат-контексте матчатся по базовому тикеру (`services/symbols.py::base_ticker`, `BTC-USDT→BTC`) с фоллбэками (текст/категория). Фронт: хук `hooks/usePrediction.ts`, `low_confidence` в бейдже `AIPanel` (теперь смонтирован на `AssetPage`), виджет `AiSignalWidget` переведён с мока на реальные данные.
- **Гибридный сигнал ИИ (с 2026-06-06):** инференс перешёл с унивариантного окна close на **11-фичную матрицу признаков** (`services/features.py::build_feature_matrix`, строгий порядок `open,high,low,close,volume,rsi,macd,macd_hist,macd_signal,bb_width,bb_pos`). Индикаторы (RSI/MACD/Bollinger/SMA) считаются на чистом Python (без pandas). `apply_scaler` нормализует матрицу через `scaler.transform`; при отсутствии `scaler.pkl` — `fit_transform`-фоллбэк, при несовпадении формы (текущий `scaler.pkl` — унивариантный) — graceful degrade на сырую матрицу (см. `app/ml/README.md`). Прогноз стал **гибридным**: `services/patchtst.py::_rule_based_signal` (RSI / MACD-кросс / SMA20-vs-SMA50 → `rule_score ∈ [-1,1]`) объединяется с вероятностью PatchTST через `_combine_signals` (веса 60% модель / 40% правила, буст уверенности до 15% при согласии сигналов, кламп `[0.15, 0.85]`); объединённые вероятности проходят прежний confidence-gate (боковик сохранён). В результат добавлены поля `patchtst_prob`/`rule_score`/`signals_agree` (проброшены в `PredictionOut`). Промт Groq (`routes/chat.py::_build_system_prompt`) переписан в структурированный русскоязычный шаблон (роль FinTrack-аналитика с отказом на нефинансовые темы, блок «АНАЛИЗ АКТИВА» с гибридным сигналом, 4-5 предложений, обязательный дисклеймер). Фронт: дефолтный таймфрейм TradingView-виджета на вкладке «Про график» изменён с `D` на `60` (1H) в `components/asset/TradingViewModal.tsx` — применяется при каждом открытии (iframe ремоунтится). Тесты: `backend/tests/test_features.py` (новый), дополнены `test_patchtst.py`/`test_chat.py`, `frontend/.../TradingViewModal.test.tsx` (новый).
- **Персистентность бэкенда (с 2026-06-02, Блок A):** PostgreSQL 14 + SQLAlchemy 2.0 async (`asyncpg`) + миграции Alembic. Слой данных — `backend/app/database.py` (async engine, `get_db`, `Base`) и `backend/app/models.py` (9 моделей: User, Subscription, DashboardConfig, ChatSession, Comment, Favorite, NewsArticle, NewsReaction, NewsFavorite). Оркестрация — `docker-compose.yml` в корне (postgres + redis + backend) и `backend/Dockerfile`. На этой машине host-порт Postgres = **5433** (5432 занят нативным PostgreSQL); внутри compose-сети — `postgres:5432`. Конфиг расширен полями `database_url`, `secret_key`, `algorithm`, `access_token_expire_minutes`, `uploads_dir` (auth-поля — задел под будущие блоки).
- **Профиль + подписка (с 2026-06-02, Блок C+G):** модуль профиля пользователя и демо-подписки. Backend — роуты `backend/app/routes/profile.py` (prefix `/users`: `GET/PATCH /me`, `check-username`, `POST /me/avatar` — Pillow center-crop 200×200 в `uploads/avatars/{id}.jpg`) и `subscription.py` (prefix `/subscription`: `status`/`upgrade` (демо-premium 30 дн)/`cancel`). Лимиты/фича-флаги выводятся из `plan` в коде (free=5 / premium=9999+всё true), в БД не хранятся; добавлена колонка `Subscription.ai_requests_used` (миграция `3b1f7c2a9d04`). `main.py` монтирует статику `/uploads` (StaticFiles) и расширяет CORS до `PATCH`. Frontend — светлая дизайн-система (НЕ MUI): `lib/profileApi.ts`, хуки `useProfile`/`useSubscription` (auth-backed, без `useMock`), `AuthContext.updateUser(partial)`, компоненты `components/profile/` (ProfileHero+blobs, AvatarUploader, ProfileEditCard) и общий `components/ui/SubscriptionCard.tsx` (Free/Premium-переключатель, цены в ₽: 990₽/мес), переписанная `ProfilePage` (Hero+2 карточки) и `SubscriptionPage` (на бэкенд, без localStorage/эмодзи), сайдбар с реальным аватаром (self-hosted `/uploads`, fallback — CSS-инициал) и всплывающим меню. Vite-proxy `/users|/subscription|/uploads → :8000`. Тесты — `backend/tests/test_profile.py` (7 кейсов, зелёные).
- **Аутентификация (с 2026-06-02, Блок B):** JWT в HttpOnly-cookie + Google OAuth. Backend — модуль `backend/app/auth/` (`utils` bcrypt+jose, `schemas`, `dependencies` `get_current_user`/`require_admin`, `router` `/auth/register|login|logout|me|google|google/callback`). Хэш паролей — библиотека **`bcrypt` напрямую** (passlib 1.7.4 несовместим с bcrypt 5.x). `EmailStr` требует `email-validator`. Конфиг: `google_client_id/secret`, `backend_url`, `frontend_url` (пустой client_id → `/auth/google` отдаёт 501). Frontend — сессия через **React Context** `frontend/src/context/AuthContext.tsx` (`useAuth`) + `frontend/src/lib/authApi.ts` (НЕ Redux — отклонение от промта в пользу конвенций проекта); guard `RoutesGuard` на `useAuth`; формы Login/Register ходят на `/auth/*`; vite-proxy `/auth → :8000`. UI — дизайн-система (НЕ MUI). `AuthProvider` зеркалит сессию в legacy localStorage-ключи (`fintrack_is_authenticated`/`fintrack_user`) для обратной совместимости.

- **Восстановление пароля (с 2026-06-11):** flow `/forgot-password` → `/reset-password?token=...`. Backend: `POST /auth/forgot-password` (токен `secrets.token_urlsafe(32)` в Redis `password_reset:{token}` TTL 900с, нейтральный ответ — наличие аккаунта не раскрывается) и `POST /auth/reset-password` (валидация токена, bcrypt-хэш, удаление токена). Письма — `services/email.py` (fastapi-mail, HTML-шаблон с акцентом #E11D48); без `SMTP_HOST` ссылка пишется в DEBUG-лог. Переменные SMTP_* и шаблон env — `backend/.env.example`. **Пин `starlette>=0.40,<0.49` обязателен** (fastapi-mail тянет starlette 1.x, несовместимый с fastapi 0.12x). Frontend: страницы `ForgotPasswordPage`/`ResetPasswordPage` (публичные роуты), ссылка «Забыли пароль?» в LoginPage, `apiForgotPassword`/`apiResetPassword` в `lib/authApi.ts`. Тесты: `backend/tests/test_password_reset.py` (7 кейсов, FakeRedis + AsyncMock email).
- **Удаление биржевых индексов (с 2026-06-11):** тип `'index'` убран из `Asset['type']` (остались crypto/stock/forex) — нет бесплатного API-источника. Удалены 7 записей из `data/prices.json` (SPX/IXIC/DJI/DAX/NKY/USOIL/UKOIL), поле `indices` из `usePrices`, ветки `type === 'index'` из `useAssetPrice`/`SimpleChart`, вкладка «Индексы» из MarketOverview/AssetTable/TopMovers, метка из PersonalizedPanel.
- **Финальная серия фиксов (с 2026-06-24):** план `.ai-factory/plans/fixes-final-series.md`. **Производительность:** `useNews`/`useOHLCV`/`useCoinInfo` получили `refetchOnMount:false` (+ `keepPreviousData` у useNews) — возврат на дашборд берёт кэш QueryClient. **Графики:** корневая причина пустых Recharts для акций/металлов — `yfinance` отсутствовал в `.venv` (есть в requirements); установлен, в `candles.py` добавлен явный ImportError-лог. **Реальные данные виджетов:** новый прокси `GET /api/quotes/global` (`coingecko.get_global()`, Redis) + хук `useGlobalMarket` для market_volume/global_market_cap (раньше браузер бил в api.coingecko.com напрямую); `gas` при отсутствии `ETHERSCAN_API_KEY` тянет keyless on-chain RPC (`eth_gasPrice`, `source=rpc`); funding_rate расширен 6→12 символов; forex 4→15 реальных пар Frankfurter (убран фейковый %change и невалидный RUB); stock_screener 6→16 тикеров; top_movers сделан детерминированным; liquidations/whale_tracker остаются помеченными «Demo» (нет бесплатного источника). **Глобальная валюта:** все места вывода цены/капы/объёма переведены на единые `formatPrice`/`formatVolume`/`formatMarketCap` (валюта-aware), капа везде компактная B/M/T. **Избранное:** задействована модель `Favorite` — новый роут `/favorites` (GET/POST/DELETE) + хук `useFavorites` + звезда на `AssetHeader` (персист); `WatchlistPanel` показывает только избранное с CTA; `NewsWidget` — явный переключатель «Все ⇄ Избранное» (по умолчанию «Все»). **Админка:** маскированный ключ из БД показывается как placeholder (value=черновик); комментарии кликабельны → deep-link `/news/{article_id}#comment-{id}` (бэкенд резолвит article_id из article_url). Поиск-новость на дашборде теперь открывает `/news/:id`. **Чистка:** виджет `CommunityWidget` удалён целиком (clampWidgets отбрасывает осиротевший тип); подписи размера в AddWidgetModal перенесены под название. **Перевод новостей:** 3-й fallback-провайдер `Polza AI` (OpenAI-совместимый) встроен в цепочку OpenRouter→Groq→Polza (`_polza_complete`, config `polza_*`, `get_api_key("polza")`); ключ в `backend/.env` (gitignored).

- **Фиксы round 2 (с 2026-06-24, фазы D–H):** план `.ai-factory/plans/fixes-round2.md`. **Уведомления (D1):** `useNotifications` один раз после авторизации шлёт `POST /api/notifications/read-all` (module-level guard по `user.id` → один read-all на загрузку приложения), счётчик колокольчика обнуляется каждый визит. **Производительность (E1):** содержимое виджета (и его data-хуки) монтируется лениво через IntersectionObserver (`WidgetCard.tsx`, rootMargin 200px, observer отключается после первого показа) — всплеск запросов на маунте дашборда размазан; QueryClient уже настроен (staleTime/gcTime/retry в `main.tsx`). **Тема и UI (F1/F2):** углублён сине-чёрный фон тёмной темы + контраст/мягкие границы (`index.css` `html[data-theme="dark"]`, светлая тема не тронута); кнопка «новый дашборд» — круглая с lucide `Plus` (`DashboardTabs.tsx`) вместо почти невидимого «+». **ИИ-чат и письма (G1–G4):** убраны упоминания «FinTrack» из системных промтов (`routes/chat.py`) и письма сброса пароля (`services/email.py`: тема/шапка/`MAIL_FROM_NAME`); быстрые блоки `ChatPage.tsx` → «Свежие новости / Растущие активы / Падающие активы / Индекс страха и жадности» с осмысленными запросами; анимированный placeholder заменён статичным. **Восстановление пароля (H1):** новый `components/auth/CodeInput.tsx` — 6 квадратных полей (авто-переход, backspace-навигация, стрелки, paste кода целиком), интегрирован в `ResetPasswordPage` с подписью «Отправили на &lt;email&gt;» (бэкенд не менялся).

- **Фиксы round 3 (с 2026-06-24):** план `.ai-factory/plans/fixes-round3.md`. **Тема и активы (A):** тёмная тема удалена полностью — приложение всегда светлое (`SettingsContext` без `theme`/`setTheme`, `index.css` без `html[data-theme="dark"]`, секция темы убрана из `SettingsPage`, `ProfileHero` без theme-веток); иконки активов возвращены в `MarketTicker` и в плотном режиме (компактные); актив `WMT` удалён отовсюду (prices.json/usePrices/StockScreener/TradingViewModal); `BNB` (нет данных → 0.00) заменён на `ADA` в корреляциях/тикере и на `ATOM` в funding. **Виджеты (B):** `PriceChartWidget` получил скрытый `YAxis domain=[dataMin,dataMax]` — без него Recharts ставил домен `[0,max]` и график выглядел «плоским»; `TechnicalAnalysisWidget` считает градуированный RSI + MACD + тренд SMA20/SMA50 (как `features.py`) — вердикт перестал залипать на «Нейтрально»; `services/gas.py` keyless-RPC перебирает несколько публичных нод с User-Agent + берёт номер блока (и пробуется при ошибке Etherscan); `services/fng.py` кэширует ровно до следующего дневного апдейта alternative.me (`time_until_update`+буфер, кламп) и отдаёт `timeUntilUpdate` (значение 17 в демо — реальный Extreme Fear, не залипший кэш). **ИИ-чат (C):** новый `services/movers.py` + `GET /api/quotes/top-movers` + Groq-tool `get_top_movers` (реальные топ растущие/падающие за сегодня по крипте OKX и акциям Finnhub, карточки); 4-й быстрый блок `ChatPage` «Индекс страха и жадности» → «Что ты умеешь» (отвечает локально каноническим текстом без модели, `useGroqChat.sendLocalAnswer`). **Данные/перф (D):** одноразовый `backend/scripts/clear_comments.py` (удаляет comment_reactions → comment-уведомления → comments, FK-безопасно, с подтверждением); хит-зона ресайза `.react-resizable-handle` 20→30px + вытянутые боковые ручки; `WidgetCard` монтирует виджеты батчами (3/120мс) поверх ленивого IntersectionObserver-маунта — всплеск одновременных fetch ограничен. Тесты: `test_quotes_new.py` (gas RPC/fallback, fng TTL, top-movers), `test_chat.py` (tool get_top_movers).

## Нефункциональные требования

- **Логирование:** verbose `console.debug('[ИмяКомпонента] ...')` во всех хуках и ключевых компонентах — обязательное соглашение, упрощает отладку демо.
- **Обработка ошибок:** каждый хук возвращает `{ data, isLoading, error }`; fallback на mock при ошибке внешнего API; компоненты рендерят empty state при отсутствии данных.
- **Безопасность:** ключи API только в `.env` (`VITE_*`), Vite-прокси для serverless-эндпоинтов; никогда не коммитить `.env` (он уже в `.gitignore`).
- **Производительность:** TanStack Query кэширует (`staleTime: 30мин` для статичных coin info), `useMemo`/`useCallback` для тяжёлых вычислений, мемоизация selector'ов.
- **Совместимость:** разработка на Windows 10 + PowerShell; пути в коде только относительные / POSIX-стиль.
- **Версионирование persisted state:** при изменении схемы виджет-реестра — бамп ключа `localStorage` + явное удаление legacy-ключей в `loadWidgets()`.

## Архитектура

Подробные правила архитектуры см. в `.ai-factory/ARCHITECTURE.md`.
Паттерн: **Feature-based Modular** (React SPA, вертикальные срезы по фичам + горизонтальные модули hooks/types/utils/lib).
