# План: Починка навигационных tools ИИ-чата (FinTrack)

**Дата создания:** 2026-06-22
**Ветка:** master (ветка не создавалась — `git.create_branches: false`)
**Режим:** Full
**Источник:** `promt.md`

> Контекст: tools `search_news` / `get_asset` в общем чате (`symbol="general"`) реализованы,
> но не работают — модель печатает сырой текст вызова, а сами инструменты не находят данные.
> Тематический guardrail работает корректно — его **НЕ трогать**.

## Settings

- **Testing:** да, для критичных исправлений (парсинг сырого вызова, нормализация крипто-тикера, двуязычный поиск). Остальное — ручная проверка по сценариям из промта.
- **Logging:** verbose — `logger.debug('[module] ...')` на бэкенде (обязательное соглашение проекта); ключевые ветки парсинга/нормализации логировать на DEBUG, протечку сырого вызова — на WARN.
- **Docs:** warn-only — без обязательного чекпоинта документации (`WARN [docs]`).

## Roadmap Linkage

- **Milestone:** none
- **Rationale:** `.ai-factory/ROADMAP.md` отсутствует — линковка пропущена.

## Контекст (проверено по коду)

- **#1/#2** `groq_service.py::get_groq_response_with_tools` (стр. 96-173) — `temperature=0.7` в payload (стр. 127). При no-tool-calls возвращает `msg.content` как есть (стр. 144-147); при exhausted rounds — `_FALLBACK_MESSAGE`. Очистки content от псевдо-синтаксиса нет нигде → сырой `<function=...>` протекает в `reply` и через `chat.py::chat_message` (стр. 626 `_merge_messages`) сохраняется в `session.messages`.
- **#3** `chat.py::_tool_get_asset` (стр. 485-512) зовёт `get_candles(symbol, "1D", 2)` напрямую. `candles.py::classify_symbol` (стр. 67-80): без дефиса → `"stock"` → yfinance → пусто для «BTC». Маппинг символов есть: `search.py:26-59` грузит `frontend/src/data/prices.json` (кэш на процесс), где crypto-активы имеют `symbol` вида `"BTC-USDT"`. `services/symbols.py::base_ticker` делает обратное (`BTC-USDT→BTC`).
- **#4** `chat.py::_tool_search_news` (стр. 460-482) ищет по `title`/`title_ru`/`description` (нет `description_ru`, нет `symbols[]`). Образец JSONB-матча с graceful-фоллбэком на sqlite — `_get_news_context` (стр. 217-249).
- **#5** Enrichment (OpenRouter→Groq фоллбэк) чинён в предыдущем плане. Готовый механизм переобогащения уже есть: `POST /api/news/reenrich` (admin, `news.py:571`) → `news_fetcher.py::reenrich_unprocessed` (стр. 324), который таргетит **`title_ru IS NULL`** (НЕ `ai_processed=False` — коммент стр. 327-330). Нового кода не требуется — только прогон.
- **#6** `chat.py::_GENERAL_SYSTEM_PROMPT` (стр. 316-328) велит «в конце ответа по сути ВСЕГДА добавляй дисклеймер» → дисклеймер на каждой реплике. Asset-чат дисклеймер аппендит хардкодом в `chat_message` (стр. 621-623) — там оставить как есть.

---

## Tasks (по фазам)

### Фаза 1 — Протечка сырого вызова функции (ПРОБЛЕМА 1, 🔴)
- [x] **#1** Понизить `temperature` tools-вызова с 0.7 до ~0.2 — `groq_service.py`
- [x] **#2** Пост-обработка content: распарсить `<function=...>`/`<function>...</function>` и выполнить через `tool_runner` с переинжектом результата в цикл (фоллбэк к структурному пути), иначе вырезать фрагмент. Гарантии: (1) сырой синтаксис не попадает ни в `reply`, ни в `session.messages` во всех ветках; (2) тем же стриппером санировать загруженную из БД `history` перед отправкой в Groq (старый мусор не должен переучивать модель) — `groq_service.py` *(blockedBy #1)*

### Фаза 2 — Данные tools не находятся (ПРОБЛЕМЫ 2 и 3, 🔴)
- [x] **#3** Нормализация крипто-тикера в пару OKX (`BTC→BTC-USDT`) через маппинг `prices.json`; не найден после нормализации → карточка остаётся, текст «цена недоступна» — `chat.py`/`symbols.py`
- [x] **#4** Двуязычный поиск новостей: `title`/`title_ru`/`description`/`description_ru` + `symbols[]`, ilike/частичное, graceful на sqlite — `chat.py`
- [ ] **#5** Переобогащение текущей базы через готовый `POST /api/news/reenrich` (`title_ru IS NULL` → перевод; предусловие — `GROQ_API_KEY` есть), чтобы русские запросы давали результат — кода не меняем, только прогон *(blockedBy #4)*

### Фаза 3 — Дисклеймер (ПРОБЛЕМА 4, 🟡)
- [x] **#6** Смягчить `_GENERAL_SYSTEM_PROMPT`: дисклеймер только при рыночной/ценовой аналитике или прогнозе, не на отказы/приветствия/термины; guardrail темы не трогать — `chat.py`

### Фаза 4 — Тесты
- [x] **#7** Unit-тесты критичных фиксов (парсинг/вырезание сырого вызова, нормализация тикера, двуязычный поиск) — `backend/tests/` *(blockedBy #2, #3, #4)*

---

## Commit Plan (чекпоинты)

1. **fix(chat):** убрать протечку сырого `<function=...>` в ответ и историю + понизить temperature tools — после #1, #2
2. **fix(chat):** нормализация крипто-тикера в get_asset + двуязычный поиск новостей — после #3, #4
3. **chore(news):** переобогащение базы (перевод) для русских запросов — после #5
4. **fix(chat):** условный дисклеймер в общем чате — после #6
5. **test(chat):** покрытие критичных фиксов tools-чата — после #7

## Ручная проверка (сценарии из промта)

После реализации прогнать в общем чате (`symbol="general"`):
1. «Найди новости о биткоине» → новость находится и показывается карточкой-ссылкой.
2. «Какая цена биткоина?» → цена BTC реально возвращается карточкой.
3. «расскажи про финансы» → ответ без дисклеймера на каждой реплике.
4. «давай про детей» → нефинансовая тема по-прежнему отбивается (guardrail).
Во всех случаях: сырого `<function=...>` в чате и в сохранённой истории нет ни разу.

В конце — отчёт: какие файлы изменены и результат ручной проверки.
