# План: Фиксы round 3 (правки после демо)

**Создан:** 2026-06-24
**Ветка:** master (создание веток отключено — `git.create_branches: false`)
**Источник:** `ПРОМТ_фиксы_round3.md`
**Проект:** FinTrack — FastAPI + PostgreSQL + Redis (backend) / React 19 + TS + Vite (frontend)

## Настройки

- **Тесты:** да, для критичных бэкенд-правок (get_top_movers, fng TTL, gas-источник). Фронт — без новых тестов, но обновить сломанные существующие.
- **Логирование:** verbose — `console.debug('[ИмяКомпонента] ...')` во фронте, `logger.debug/warning` в бэкенде (обязательная конвенция проекта).
- **Документация:** warn-only (без обязательного docs-чекпоинта). Заметку о фиксах добавить в `DESCRIPTION.md` при финальном коммите как обычно.
- **Конвенции:** graceful degradation, не ломать рабочее (Google-вход, перевод новостей, drag-and-drop), ключи — через `get_api_key` (БД→.env). После каждой фазы — самопроверка и коммит.

## Roadmap Linkage

Milestone: "none"
Rationale: ROADMAP.md отсутствует — линковка пропущена.

## Приоритет на защиту

A и B (внешний вид + рабочие виджеты) → C (чат) → D (данные/перф).

---

## Задачи по фазам

### Фаза A — 🔴 Светлая тема и активы

- **Задача #1 — A1: Только светлая тема.** `SettingsContext.tsx` (убрать/зафиксировать `light`, снять `data-theme="dark"` на `<html>`), `index.css` (удалить `html[data-theme="dark"]{...}` и тёмные оверрайды), убрать переключатель темы в настройках, поправить `SettingsContext.test.tsx`.
- **Задача #2 — A2: Иконки в MarketTicker.** `MarketTicker.tsx` — вернуть иконки активов (см. git-историю файла), сохранив исправленную раскладку (символ с ellipsis, цена не наезжает).
- **Задача #3 — A3: Убрать WMT.** `data/prices.json`, `usePrices.ts`, `StockScreenerWidget.tsx`, `TradingViewModal.tsx` и прочие маппинги. Грепом проверить отсутствие WMT.
- **Задача #4 — A4: Заменить BNB.** `CorrelationMatrixWidget.tsx`, `MarketTicker.tsx`, `FundingRateWidget.tsx` — заменить BNB на существующий актив (ADA/LINK). Проверить отсутствие пустых столбцов в матрице корреляций.

### Фаза B — 🔴 Виджеты, которые не работают

- **Задача #5 — B1: График цены.** `PriceChartWidget.tsx` — строить из реальных свечей (`useOHLCV`), убрать mock; обновить `__tests__/PriceChartWidget.test.tsx`.
- **Задача #6 — B2: Теханализ.** `TechnicalAnalysisWidget.tsx` — реальные RSI/MACD/тренд по OHLCV (логика как `services/features.py`), меняющийся вердикт.
- **Задача #7 — B3: Gas-трекер.** `services/gas.py` — починить живой RPC-источник (рабочий публичный RPC/другой бесплатный), обновление по TTL; `GasTrackerWidget.tsx` — снимать пометку «демо» при живых данных. + тест парсинга/фоллбэка.
- **Задача #8 — B4: Fear & Greed.** `services/fng.py` — починить TTL (значение залипло на 17), реальный апдейт с alternative.me. + тест парсинга и TTL/фоллбэка.

### Фаза C — 🔴 ИИ-чат: инструменты и быстрые действия

- **Задача #9 — C1: get_top_movers.** Backend: сервис/эндпоинт, агрегирующий реальные котировки по всем классам (okx/finnhub/frankfurter — источники обзора рынка), сортировка по % за сегодня, карточки. Tool в `routes/chat.py` (`_CHAT_TOOLS` + `_tool_get_top_movers` + `_runner`). + тесты в `test_chat.py`.
- **Задача #10 — C2: Чип «Что ты умеешь».** `ChatPage.tsx` — заменить 4-й чип (Fear&Greed) на «Что ты умеешь» (lucide-иконка), прямой ответ о возможностях; растущие/падающие чипы работают через tool из C1. _(blocked by #9)_

### Фаза D — 🟡 Данные, дашборд, производительность

- **Задача #11 — D1: Очистка комментариев.** Безопасный одноразовый скрипт (как `_drop_subscriptions`) или админ-действие: удалить `comments` + `comment_reactions` + reaction-уведомления; FK-порядок; данные юзеров/новостей не трогать.
- **Задача #12 — D2: Зона ресайза.** `index.css` `.react-resizable-handle-*` — увеличить хит-зону ~20px → ~28–32px без визуальных маркеров; не ломать drag за шапку.
- **Задача #13 — D3: Перф дашборда.** TanStack Query (staleTime/keepPreviousData), ограничение параллелизма fetch, ленивая подгрузка вне вьюпорта (`WidgetCard.tsx` IntersectionObserver), мемоизация. `main.tsx`, `hooks/*`, `WidgetCard.tsx`.

---

## План коммитов

1. **feat(ui):** только светлая тема (убрать dark), вернуть иконки тикера — задачи #1, #2
2. **fix(assets):** убрать WMT, заменить BNB на существующий актив — задачи #3, #4
3. **fix(widgets):** реальный график цены, рабочий теханализ, живой gas, обновляемый Fear&Greed — задачи #5–#8
4. **feat(chat):** инструмент топ-муверов + чип «Что ты умеешь» — задачи #9, #10
5. **chore(db):** очистка всех комментариев — задача #11
6. **perf(dashboard):** больше зона ресайза + оптимизация открытия — задачи #12, #13

После каждого коммита — самопроверка (тесты бэкенда зелёные, фронт собирается/линт). В конце — короткий отчёт.
